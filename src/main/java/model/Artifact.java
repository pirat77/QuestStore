package model;

public class Artifact extends Element {
    public Artifact(int id, String name, String description, int value, int categoryId) {
        super(id, name, description, value, categoryId);
    }
}
