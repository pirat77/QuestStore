package model;

public class Quest extends Element {

    public Quest(int id, String name, String description, int value, int categoryId) {
        super(id, name, description, value, categoryId);
    }
}
